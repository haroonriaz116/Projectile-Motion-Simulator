# Projectile-Motion-Simulator
Projectile-Motion-Simulator is a Python and Pygame application that visually animates projectile motion based on user-entered launch velocity, angle, and height. It calculates the projectile's range using standard kinematics equations, renders a real-time animation of the trajectory, and lets users run repeated calculations interactively.
